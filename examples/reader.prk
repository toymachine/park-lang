function main()
{
    let read_all = (r) => {
        if(r) {
            print("frsT: ", first(r))
            recurs (next(r))
        }
    }
    let read = (s) => {
        read_all(reader(s))
    }

    read(slurp("./examples/sexpr.clj"))
}